# Foundry (forge/cast/anvil) — kept in .zshenv (not .zprofile/.zshrc) so it's
# on PATH even for non-interactive/non-login shell invocations (IDE tasks, scripts).
export PATH="$PATH:$HOME/.foundry/bin"
